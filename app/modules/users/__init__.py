"""Users feature module (user management + Shop domain).

NOTE: The `Shop` model currently lives here because it belongs to a user
(owner). Give it its own ``app.modules.shop`` module once shop routes are added.
"""